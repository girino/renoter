# Renoter Configuration
# Copy this file to .env and adjust the values

# Renoter Server Configuration
# Private key in hex format (leave empty to generate new one)
RENOTER_PRIVATE_KEY=

# Comma-separated list of relay URLs for listening and forwarding
# The same relays are used for both listening and forwarding
RENOTER_RELAYS=wss://relay1.example.com,wss://relay2.example.com

# Renoter Client Configuration
# Comma-separated list of Renoter npubs (path through Renoters)
RENOTER_PATH=npub1example...,npub2example...

# Comma-separated list of relay URLs where wrapped events will be sent
CLIENT_SERVER_RELAYS=wss://relay1.example.com,wss://relay2.example.com

# Client listen address and port
CLIENT_LISTEN=:8080

# Verbose logging (optional)
# Set to "true" or "all" for all debug logging
# Or specify modules: "client.wrapper,server.handler"
VERBOSE=
